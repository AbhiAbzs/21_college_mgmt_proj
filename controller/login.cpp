#include <iostream>
using namespace std;

//My Inclusion
#include "declerations/login.hh"

bool Login::isLoggedIn(int n)
{
    //needs to edit it.
    switch (n)
    {
    case 1:
        if (n = 1)
        {
        }

        break;
    default:
        cout << "please choose correct option from the above list:-" << endl;
    }
}